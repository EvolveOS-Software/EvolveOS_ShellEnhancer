<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EvolveOS_ShellEnhancer.Views.RunWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Run">

    <Grid>
        <!-- Optional outer border for neatness -->
        <Border Background="Transparent" CornerRadius="8" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid Padding="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Title Bar -->
                    <RowDefinition Height="*"/>
                    <!-- Content -->
                    <RowDefinition Height="Auto"/>
                    <!-- Buttons -->
                </Grid.RowDefinitions>

                <!-- Custom Drag Title Bar -->
                <Grid Grid.Row="0" Background="Transparent"
                      PointerPressed="TitleBar_PointerPressed"
                      PointerMoved="TitleBar_PointerMoved"
                      PointerReleased="TitleBar_PointerReleased"
                      Padding="16,12,16,12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE78B;" FontSize="16"/>
                        <TextBlock Text="Run" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Input Area -->
                <StackPanel Grid.Row="1" Padding="24,12,24,12" Spacing="16">
                    <TextBlock Text="Type the name of a program, folder, document, or Internet resource, and Windows will open it for you." 
                               TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Open:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,16,0"/>
                        <TextBox x:Name="InputTextBox" Grid.Column="1" VerticalAlignment="Center" KeyDown="InputTextBox_KeyDown" PlaceholderText="cmd, explorer, control..."/>
                    </Grid>
                </StackPanel>

                <!-- Footer Buttons -->
                <Grid Grid.Row="2" Background="{ThemeResource LayerFillColorAltBrush}" Padding="24,16" CornerRadius="0,0,8,8" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                        <Button x:Name="BtnOk" Content="OK" Width="80" Style="{StaticResource AccentButtonStyle}" Click="BtnOk_Click"/>
                        <Button x:Name="BtnCancel" Content="Cancel" Width="80" Click="BtnCancel_Click"/>
                        <Button x:Name="BtnBrowse" Content="Browse..." Width="90" Click="BtnBrowse_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>